{
  "name": "acortador-url-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode production && cp public/signup.html dist/ && cp public/login.html dist/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "jwt-decode": "^4.0.0"
  }
}
